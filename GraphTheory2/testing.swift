//
//  main.swift
//  GraphTheory2
//
//  Created by Mike Watson on 11/28/24.
//

/* This file is for testing purposes. To check tests, rename this file to main.swift,
and uncomment the following code. Make sure to also comment out @main from GraphTheory2App.swift. */

/*
import Foundation

struct TestGraph1 {
    let a = Vertex()
    let b = Vertex()
    let c = Vertex()
    let edgeAB: Edge
    let edgeBC: Edge
    let graph: Graph
    
    init() {
        edgeAB = Edge(startVertexID: a.id, endVertexID: b.id)
        edgeBC = Edge(startVertexID: b.id, endVertexID: c.id)
        graph = Graph(vertices: [a, b, c], edges: [edgeAB, edgeBC])
    }
}

print("Testing if TestGraph1 contains a cycle. Output should be false.")
print(TestGraph1().graph.hasCycle())

print("Checking if K_3,3 contains a cycle. Output should be true.")
print(K33().graph.hasCycle())
*/
